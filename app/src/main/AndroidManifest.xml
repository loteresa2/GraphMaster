<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.applicationcourse.mobile.graphmaster">

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.ReadingActivity"
            android:label="@string/title_activity_reading"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.DrawGraphActivity"
            android:label="@string/title_activity_draw_graph"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.ShowTextActivity"
            android:label="@string/title_activity_show_text"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UI.LevelActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".UI.Explosion"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".UI.ListActivity"
            android:label="@string/list_name"
            ></activity>
    </application>

</manifest>
